@model Biblioteca_Mia_Raymundo.Models.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Iniciar Sesión";
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>Login - Biblioteca</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        /* Estilos generales */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #fbc2eb, #a6c1ee); /* Colores suaves de gradiente */
            height: 100vh;
        }

        .center-wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .login-card {
            background-color: #fff;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
        }

            .login-card h2 {
                text-align: center;
                margin-bottom: 30px;
                color: #5d3fd3; /* Color morado suave */
            }

            .login-card label {
                color: #5d3fd3; /* Color morado suave */
                font-weight: 600;
            }

            .login-card input {
                border: 1px solid #e0e0e0;
                border-radius: 8px;
                padding: 12px;
                width: 100%;
                margin-bottom: 20px;
                transition: border 0.3s;
            }

                .login-card input:focus {
                    border-color: #f79c42; /* Naranja suave */
                    box-shadow: 0 0 5px rgba(247, 156, 66, 0.5);
                    outline: none;
                }

            .login-card button {
                width: 100%;
                padding: 12px;
                background-color: #f79c42; /* Naranja suave */
                color: white;
                border: none;
                border-radius: 8px;
                font-size: 16px;
                cursor: pointer;
                transition: background 0.3s ease-in-out;
            }

                .login-card button:hover {
                    background-color: #f67e0e; /* Naranja más oscuro */
                }

        .register-link {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
        }

            .register-link a {
                color: #5d3fd3; /* Color morado suave */
                text-decoration: none;
            }

                .register-link a:hover {
                    text-decoration: underline;
                }

        /* Estilos para la validación de errores */
        .validation-summary-errors {
            margin-bottom: 20px;
            color: #e74c3c;
        }
    </style>
</head>
<body>
    <div class="center-wrapper">
        <div class="login-card">
            <h2>Iniciar Sesión</h2>
            <form asp-action="Login" method="post">
                <div asp-validation-summary="All" class="validation-summary-errors"></div>
                <div>
                    <label asp-for="UserName">Usuario:</label>
                    <input asp-for="UserName" class="form-control" placeholder="Ingresa tu usuario" />
                    <span asp-validation-for="UserName" class="text-danger"></span>
                </div>
                <div>
                    <label asp-for="Password">Contraseña:</label>
                    <input asp-for="Password" class="form-control" placeholder="Ingresa tu contraseña" type="password" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div>
                    <button type="submit">Entrar</button>
                </div>
            </form>
            <div class="register-link">
                <p>¿No tienes cuenta? <a asp-controller="Usuario" asp-action="Registro">Regístrate aquí</a></p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</body>
</html>
